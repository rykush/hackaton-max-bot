import { Button, Panel, Container, Typography } from '@maxhub/max-ui';

const Search = () => (
    <Button
    appearance="themed"
    mode="primary"
    onClick={() => {}}
    size="medium"
    >
    Поиск
    </Button>
);

export default Search;